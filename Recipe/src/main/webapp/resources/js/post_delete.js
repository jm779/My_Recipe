window.addEventListener("load", () => {
    document.getElementsByClassName("btn-delete").addEventListener("click", e => {
        const delete_btn = document.querySelectorAll(".btn-delete")
    })
});