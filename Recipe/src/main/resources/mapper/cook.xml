<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cook">
	<select id="list" resultType="cook">
		SELECT * 
		FROM cook
	</select>
	
   	<insert id="add" parameterType="cook">
		INSERT INTO cook(recipeid, recipetitle, "comment", link)
		VALUES (cook_seq.nextval, #{recipetitle}, #{comment}, #{link})
	</insert>
	
	<select id="item" resultType="Cook">
		SELECT * 
		FROM cook
		WHERE recipeid = #{recipeid}
	</select>
	
	<update id="update" parameterType="cook">
		UPDATE cook
		SET recipetitle = #{recipetitle},
			"comment" = #{comment},
			link = #{link}
			WHERE recipeid = #{recipeid}	
	</update>
	
	<delete id="delete">
		DELETE FROM cook
		WHERE recipeid = #{recipeid}
	</delete>
	
	<select id="detail" resultType="cook">
		SELECT * 
		FROM cook
		WHERE recipeid = #{recipeid}
	</select>
	
</mapper>