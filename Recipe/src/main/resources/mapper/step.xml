<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="step">

	<resultMap id="StepResultMap" type="kr.ac.kopo.recipe.model.Step">
		<id property="stepid" column="stepid" />
		<result property="recipeid" column="recipeid" />
		<result property="stepOrder" column="stepOrder" />
		<result property="content" column="content" />
		<result property="ingredient" column="ingredient" />
		<result property="tools" column="tools" />
		<result property="tip" column="tip" />
		<result property="imagepath" column="imagepath" />
	</resultMap>

	<insert id="add" parameterType="Step">
		INSERT INTO step(recipeid, step_order, content, ingredient, tools, tip, imagepath)
		VALUES (#{recipeid}, #{stepOrder}, #{content}, #{ingredient}, #{tools}, #{tip}, #{imagepath})
	</insert>

	<select id="stepitem" parameterType="int" resultType="Step">
		SELECT *
		FROM step
		WHERE recipeid = #{recipeid}
		ORDER BY step_order
	</select>
	
	<update id="update">
		UPDATE step
		SET content = #{content},
			ingredient = #{ingredient},
			tools = #{tools},
			tip = #{tip}, 
			imagepath = #{imagepath}
		WHERE stepid = #{stepid}	
	</update>

	<delete id="deleteByRecipeId" parameterType="int">
		DELETE FROM step 
		WHERE recipeid = #{recipeid}
	</delete>
	
	<select id="listByRecipeId" parameterType="int" resultType="Step" >
		SELECT * FROM step
		WHERE recipeid = #{recipeid}
		ORDER BY step_order
	</select>


</mapper>